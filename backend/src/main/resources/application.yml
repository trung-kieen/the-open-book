spring:
  profiles:
    active: dev
  application:
    name: the-open-book


# Threshold for file uploading
servelet:
  multipart:
    max-file-size: 50MB
springdoc:
  default-produces-media-type: application/json

server:
  servelet:
    context-path: /api/v1/
